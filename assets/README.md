# Assets owned by Skeletons

`icons/`   tab and launcher artwork shipped with this environment
`skins/`   optional CSS extras
`fonts/`   optional local fonts

REAPER's own track icons stay in the folder configured under Settings
(typically `~/.config/REAPER/Data/track_icons`). The GTK apps search
environment assets first, then that REAPER folder.
